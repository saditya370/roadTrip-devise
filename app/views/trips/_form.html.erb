<%= form_with(model: trip,class: "trip_form") do |form| %>
  <% if trip.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(trip.errors.count, "error") %> prohibited this trip from being saved:</h2>

      <ul>
        <% trip.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

<div class="card mb-3">
  <%= image_tag (asset_path 'ontheroad.png'), class: 'card-img-top img_road' %>

  <div class="card-body">
    <h5 class="card-title">Name</h5>
    <div class="card-text">
    <%= form.text_field :name,class: "input_f" %>
    <br />
    <br />
     <div class="card-text">
      <%= form.button "Create Trip", type: "submit", :class=> "btn  btn-secondary" %>

  </div>
  </div>

  </div>
</div>
<% end %>
<style>
.trip_form{
  text-align: center;
}
.img_road{
    width: 100%;
    height: 20vw;
    object-fit: cover;
  }
.input_f{
  border: 3px solid grey;
  border-radius: 4px;
}
</style>
